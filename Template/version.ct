#include "version.h"

#ifdef DEBUG 
#define _CONFIG_STR_ "Debug"
#else 
#define _CONFIG_STR_ "Release"
#endif

uint8_t* version = (char*) "-.-" _CONFIG_STR_;
uint8_t* built_date = (char*) __DATE__;
uint8_t* built_time = (char*) __TIME__;
